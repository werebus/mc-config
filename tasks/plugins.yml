---
- name: 'Install plugins'
  copy:
    src: "files/plugins/{{ item.file }}"
    dest: "/opt/minecraft/server/plugins/{{ item.file }}"
    checksum: "{{ item.checksum }}"
    backup: yes
    owner: "{{ minecraft_user }}"
    group: "{{ minecraft_group }}"
  notify:
    - restart minecraft
  loop: "{{ bukkit_plugins }}"

- name: 'Cenotaph config'
  copy:
    src: files/cenotaph_config.yml
    dest: /opt/minecraft/server/plugins/Cenotaph/config.yml
    owner: "{{ minecraft_user }}"
    group: "{{ minecraft_group }}"
  notify:
    - restart minecraft
